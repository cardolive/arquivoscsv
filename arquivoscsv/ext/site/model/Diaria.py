class Diaria:
	def __init__(self, unidade, diaria, favorecido, destino, data_solicitacao, finalidade, situacao, valor, data_lim_prest_ctas, sit_prest_ctas, val_realizado, empenho, liquidacao, val_liquid, data_prev_pag, pagamento, data_pagam, liqu_complem, val_liq_compl, prev_pag_compl, pag_complem, data_pag_compl, recolhimento, val_boleto, data_boleto, simples, completas, centro_gerencial, data_inicio, data_fim, ambito, convenio, email):
		self.unidade = unidade
		self.diaria = diaria
		self.favorecido = favorecido
		self.destino = destino
		self.data_solicitacao = data_solicitacao
		self.finalidade = finalidade
		self.situacao = situacao
		self.valor = valor
		self.data_lim_prest_ctas = data_lim_prest_ctas
		self.sit_prest_ctas = sit_prest_ctas
		self.val_realizado = val_realizado
		self.empenho = empenho
		self.liquidacao = liquidacao
		self.val_liquid = val_liquid
		self.data_prev_pag = data_prev_pag
		self.pagamento = pagamento
		self.data_pagam = data_pagam
		self.liqu_complem = liqu_complem
		self.val_liq_compl = val_liq_compl
		self.prev_pag_compl = prev_pag_compl
		self.pag_complem = pag_complem
		self.data_pag_compl = data_pag_compl
		self.recolhimento = recolhimento
		self.val_boleto = val_boleto
		self.data_boleto = data_boleto
		self.simples = simples
		self.completas = completas
		self.centro_gerencial = centro_gerencial
		self.data_inicio = data_inicio
		self.data_fim = data_fim
		self.ambito = ambito
		self.convenio = convenio
		self.email = email

