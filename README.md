# pip freeze > requirements.txt (gera o arquivo requerimentos)

# instala as dependencias do arquivo requerimento
# Run pip install -r requirements.txt (Python 2), 
# or pip3 install -r requirements.txt (Python 3)
